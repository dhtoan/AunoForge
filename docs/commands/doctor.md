# `doctor`

Runs deterministic repository-health checks for maintenance files, tests, CI, dependency update automation, CODEOWNERS, and compatibility declarations.

```bash
npx aunoforge doctor --root .
```

The score does not use a model.
