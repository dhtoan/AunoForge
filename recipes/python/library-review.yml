{
  "schema": "aunoforge.dev/recipe/v1",
  "id": "python/library-review",
  "name": "Python Library Review",
  "version": 1,
  "trust": {"level": "builtin"},
  "applies_to": ["python"],
  "inputs": ["git_diff"],
  "passes": ["correctness","security","compatibility"],
  "checks": ["python.metadata"],
  "tools": ["filesystem.read","git.diff"],
  "permissions": {"shell": "deny","network": "deny"},
  "requires": {"provider": {"structuredOutput": true}},
  "output": {"contract": "review-report/v1"}
}
