{
  "schema": "aunoforge.dev/recipe/v1",
  "id": "general/bug-triage",
  "name": "Bug Triage",
  "version": 1,
  "trust": {"level": "builtin"},
  "applies_to": ["generic"],
  "inputs": ["github_issue"],
  "passes": ["classification","severity"],
  "checks": ["issue.required-fields"],
  "tools": ["github.issue.read"],
  "permissions": {"shell": "deny","network": "deny"},
  "requires": {"provider": {"structuredOutput": true}},
  "output": {"contract": "review-report/v1"}
}
