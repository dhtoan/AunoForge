name: Temporary config presets final runtime refresh

on:
  push:
    branches:
      - tmp/config-presets-final-runtime-43

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: corepack enable
      - run: corepack prepare pnpm@10.15.1 --activate
      - run: pnpm install --frozen-lockfile
      - run: pnpm build:action
      - name: Preserve generated CLI runtime only
        shell: bash
        run: |
          cp dist/action/cli.mjs /tmp/aunoforge-cli.mjs
          git diff --exit-code -- dist/action/index.mjs
          git checkout feat/v0.2-config-presets
          cp /tmp/aunoforge-cli.mjs dist/action/cli.mjs
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add dist/action/cli.mjs
          git diff --cached --quiet && exit 0
          git commit -m "build: refresh packaged configuration runtime"
          git push origin HEAD:feat/v0.2-config-presets
